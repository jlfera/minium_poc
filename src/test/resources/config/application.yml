# Web Drivers   
minium:
  webdriver:
    desiredCapabilities:
      browserName: chrome
    window:
      maximized: true

# Configuration that can be injected in your tests
  config:
    base_url: https://www.embratel.com.br/
    searches:
      Embratel:
        - http://www.embratel.com.br/
